# Specs for share-wgpu-adapter

This change is a pure implementation refactor to fix a memory leak. There are no new or modified capabilities, and therefore no spec-level requirements.

See `proposal.md` and `design.md` for implementation details.
